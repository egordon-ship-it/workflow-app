{
  "extends": "next/core-web-vitals",
  "rules": {
    "no-alert": "error",
    "no-restricted-globals": [
      "error",
      {
        "name": "confirm",
        "message": "Native browser confirm() is banned. Use useConfirm() from @/components/ConfirmDialog."
      },
      {
        "name": "alert",
        "message": "Native browser alert() is banned. Use react-hot-toast (toast.success / toast.error) instead."
      },
      {
        "name": "prompt",
        "message": "Native browser prompt() is banned. Build a proper input modal instead."
      }
    ],
    "no-restricted-properties": [
      "error",
      {
        "object": "window",
        "property": "confirm",
        "message": "Native browser confirm() is banned. Use useConfirm() from @/components/ConfirmDialog."
      },
      {
        "object": "window",
        "property": "alert",
        "message": "Native browser alert() is banned. Use react-hot-toast (toast.success / toast.error) instead."
      },
      {
        "object": "window",
        "property": "prompt",
        "message": "Native browser prompt() is banned. Build a proper input modal instead."
      }
    ]
  }
}
